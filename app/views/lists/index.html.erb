<h1>Lists</h1>
<hr>
<ul>
  <% @lists.each do |list| %>
    <li><%= link_to list.name, list %></li>
  <% end %>
</ul>

<p><%= link_to 'Create new list', new_list_path(@list), :class => 'btn btn-primary' %> </p>
