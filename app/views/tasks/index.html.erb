<h1> Tasks </h1>
<hr>
<div class="container" id="content">
  <% @tasks.each do |task| %>
    <%= link_to task.description, task %></br>
  <% end %>
<button class="btn btn-default btn-xs" style="float:left"><%= link_to 'New task', new_task_path(:list_id => @list.id) %></button> 
</div>
   